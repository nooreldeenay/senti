steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/$PROJECT_ID/senti-app',
      '--build-arg', 'NEXT_PUBLIC_TLDRAW_LICENSE_KEY=$_TLDRAW_KEY',
      '.'
    ]
images:
  - 'gcr.io/$PROJECT_ID/senti-app'
